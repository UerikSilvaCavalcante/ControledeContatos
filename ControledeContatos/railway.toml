[build]
builder = "dockerfile"
dockerfilePath = "./ControledeContatos/Dockerfile"

[deploy]
startCommand = "dotnet ControledeContatos.dll"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "never"
